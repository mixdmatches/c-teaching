// 比如这里放用户相关的接口函数
import service from '../utils/request'
// 所有接口函数都要以api开头，请求类型，后面跟接口名称，命名最好语义化，统一用箭头函数
// export const apiGetUserInfo = () => service.get('/user/info')
// export const apiPostUserInfo = data => service.post('/user/info', data)
